<table class="table col-md-3">
  <tr>
    <th>ジャンル検索</th>
  </tr>
  <% genres.each do |genre| %>
    <tr>
      <td><%= link_to(genre.name, items_path(genre_id: genre.id)) %></td>
    </tr>
  <% end %>
</table>