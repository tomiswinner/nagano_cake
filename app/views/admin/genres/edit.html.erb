<%= render(partial: "/layouts/err-msg") %>

<h2>ジャンル編集</h2>
<div>
  <%= form_with(model: @genre,url: admin_genre_path,local: true) do |f| %>
    <%= f.label :name,"ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit "変更を保存" %>
  <% end %>
</div>